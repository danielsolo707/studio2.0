# `supabase/`

| File | Purpose |
|------|---------|
| `schema.sql` | Full SQL schema — run in Supabase SQL Editor for a new project |

App tables: `projects`, `site_content`, `contact_messages`, `app_settings`, …
