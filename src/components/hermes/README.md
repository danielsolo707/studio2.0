# `components/hermes/`

Public and admin AI assistant UI:

- `HermesChatWidget.tsx` — chat panel
- `PublicAssistantFloatingButton.tsx` / `AdminHermesFloatingButton.tsx`
- `AiConfigForm.tsx` — model/API config (admin)
- `ChatHistory.tsx` — session history (admin)

Server agent code lives in `src/agents/hermes/`.
