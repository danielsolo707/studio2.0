# `components/dashboard/`

Admin dashboard UI, grouped by job:

```
dashboard/
├── auth/        Login, 2FA, password, captcha toggle
├── content/     Hero / About / option editors
├── projects/    Add / edit / list / upload project media
├── messages/    Contact inbox panel
├── shared/      StatusBadge, EditableSelect*
└── index.ts     Barrel re-exports
```
