# src/types/ — Shared TypeScript interfaces

| File         | Exports                                                    |
|--------------|------------------------------------------------------------|
| `project.ts` | `Project`, `ProjectLink`, `SiteContent`, `AboutSection`, `HeroSection`, `ProjectDiscipline`, `ProjectStatus`, `ProjectLinkType` |

These types are used across the entire codebase — components, lib, and pages.
